NEXT_PUBLIC_APP_URL=http://localhost:3023

# --- Local file store (default when API URL is unset) ---
# Saves to data/store.json — great for offline demos.

# --- Cloudflare Worker API + D1 (production) ---
# PRODUCTPASS_API_URL=https://api.theproductpass.garden
# PRODUCTPASS_API_KEY=your-secret-key
# Set the same PRODUCTPASS_API_KEY as a Worker secret: wrangler secret put API_KEY

# Optional: Passport Copilot AI
# OPENAI_API_KEY=sk-...
